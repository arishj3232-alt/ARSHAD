Fix final production issues and enhance UX in OnlyTwo.

IMPORTANT:
Do not break existing functionality. Apply clean fixes.

==================================================
🔥 1. FIX FIRESTORE INDEX ERROR (CRITICAL)
==========================================

Problem:
"The query requires an index"

Fix:

* Identify the query causing this (likely message search or ordering)
* Simplify query if possible OR ensure proper indexing

Implementation:

* Use single-field queries where possible
* Avoid combining multiple where + orderBy unless necessary
* Add fallback if index missing (prevent crash)

Also:

* Catch Firestore errors and prevent UI crash
* Show graceful fallback instead of error overlay

==================================================
🔗 2. SMART LINK PREVIEW + REDIRECT
===================================

Detect links in messages:

* YouTube
* Instagram
* Spotify
* Any URL

Features:

1. Auto-detect links in message text
2. Convert into clickable preview

UI:

* Show preview card:

  * title
  * domain (youtube.com, etc.)
  * thumbnail (if possible)

3. On click:

* Open in new tab (desktop)
* Open app (mobile if possible)

Implementation:

* Use regex to detect URLs
* Wrap in anchor tag
* Add preview UI component

==================================================
🔍 3. FIX SEARCH UI BUG (IMPORTANT)
===================================

Problem:
Search causing Firestore index error

Fix:

* Do NOT use complex Firestore query for search

Instead:

* Fetch messages normally
* Filter on frontend

Implementation:

* messages.filter(msg => msg.text.includes(searchTerm))

Also:

* Highlight matched text cleanly
* Scroll to result

==================================================
🛑 4. REMOVE ADMIN ICON
=======================

Problem:
Admin icon visible in top right

Fix:

* Completely remove admin icon from UI

Keep ONLY:

* Mobile trigger → typing "laura"
* Desktop trigger → CTRL + SHIFT + S

Ensure:

* No visible admin button remains

==================================================
📱 5. MOBILE LINK BEHAVIOR
==========================

* Links should open:

  * YouTube → YouTube app
  * Instagram → Instagram app
  * Spotify → Spotify app

Use:

* target="_blank"
* rel="noopener noreferrer"

==================================================
✨ 6. FINAL UX POLISH
====================

* Smooth hover for links
* Add underline + color change
* Add subtle animation on click

==================================================
💎 FINAL GOAL
=============

* No Firestore errors
* Clean search system
* Smart link previews
* Hidden admin system (no icon)
* Production-ready UX

Do not ask for confirmation.
Implement cleanly.
